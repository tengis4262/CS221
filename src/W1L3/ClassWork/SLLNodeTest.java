package W1L3.ClassWork;

public class SLLNodeTest {
    Integer a = new Integer(13);

    public static void main(String[] args) {

    }
    //Data field
    /*
    addFirst(E item)
    if(head == null)
    head = new Node(item,head)
    else
    Node nm = new Node(item,head)
    head = nm;

    addFirst(E item)
    head = new Node(item,head);

    addAfter(Node<E> after, E item){{
    after.next = new Node(item, after.next);
    size++


    addAfter(Node<E> after, E item)
    Node nm = new Node(Item, after.next);
    after.next = nm;
     */
}
